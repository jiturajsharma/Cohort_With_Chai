

// You lost phone in a list of items. Find the index of "Phone".

// Problem Statement: Create a function that finds the index of "Phone" in the list of items and returns the index.

function findPhone(items){
    items.indexOf("Phone")
    return items
}
const PhonesList = findPhone(["Wallet", "Keys", "Phone", "Glasses"])

console.log(PhonesList)

